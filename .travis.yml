language: python
dist: xenial
python:
  - "3.7"

services:
  - docker

env:
  global:
    - VERSION: 0.9.0.${TRAVIS_BUILD_NUMBER}
    - API_SERVER: https://api.lightest.xyz/testing/

script:
- bash build.sh

deploy:
  provider: script
  script: bash deploy.sh
  on:
    branch: master